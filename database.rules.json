{
  "rules": {
    ".read": true,
    ".write": true,
    "users": {
      ".indexOn": ["token", "username", "kind"]
    },
    "results": {
      ".indexOn": ["userId", "mode", "timestamp"]
    }
  }
}
